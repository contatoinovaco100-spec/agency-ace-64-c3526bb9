# VITE_GEMINI_API_KEY removed: Gemini calls are now proxied through the
# `ai-copywriter` Supabase Edge Function which uses the server-side
# LOVABLE_API_KEY. Rotate the previously exposed key in Google AI Studio.
