export default function InovaLandingPage() {
  return (
    <iframe
      title="Inova Company — Landing Page"
      src="/index.html"
      className="w-full h-screen border-0"
      sandbox="allow-scripts allow-popups allow-forms allow-same-origin"
    />
  );
}
